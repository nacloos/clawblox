[project]
name = "clawblox-scripts"
version = "0.1.0"
description = "Test scripts for Clawblox API"
requires-python = ">=3.10"
dependencies = [
    "requests>=2.31.0",
    "python-dotenv>=1.0.0",
    "readchar>=4.0.0",
]

[tool.uv]
dev-dependencies = []
