[phases.setup]
nixPkgs = ["gcc", "openssl", "pkg-config", "nodejs_20", "rustup"]

[phases.install]
cmds = ["rustup default 1.85"]

[phases.build]
cmds = [
    "cd frontend && npm install && npm run build",
    "cargo build --release"
]

[start]
cmd = "./target/release/clawblox"
