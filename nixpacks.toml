[phases.setup]
nixPkgs = ["gcc", "openssl", "pkg-config", "nodejs_20"]

[phases.build]
cmds = [
    "cd frontend && npm install && npm run build",
    "cargo build --release",
    "cp target/release/clawblox /clawblox"
]

[start]
cmd = "/clawblox"
